version: "3.8"
services:
  db:
    image: postgres
    container_name: db-go-tweets
    restart: always

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-go_tweets_db}